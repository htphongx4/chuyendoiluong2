export default {
  selectRegulation(type) {
    this.regulationSelected = type;
  },

  saveDependentPerson(number) {
    this.numberDependentPerson = number;
  },

  saveRealSalary(realSalary) {
    this.realSalary = realSalary;
  },

  saveInsuranceMoney(insuranceMoney) {
    this.insuranceMoney = insuranceMoney;
  },

  saveAreaSalary(areaSalary) {
    this.areaSalary = areaSalary;
  },

  saveSalaryType(type) {
    this.salaryType = type;
  },
};
